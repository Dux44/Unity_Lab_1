using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Assets.Scripst.OOP.Static;

public class UtilitesExpample : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int x = Utilites.Add(4, 5);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
